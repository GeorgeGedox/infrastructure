---
- name: Update all packages
  apt:
    update_cache: true
    upgrade: safe
    autoremove: true
  when: packages_update

- name: Install specified packages
  apt:
    name: "{{ item }}"
    state: present
  loop: "{{ setup_packages }}"
